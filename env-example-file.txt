# Copy this file to .env and fill in real values. Never commit .env.

# --- Postgres ---
POSTGRES_USER=fsm
POSTGRES_PASSWORD=changeme_use_a_long_random_string
POSTGRES_DB=fsm

# --- MinIO (S3-compatible storage) ---
MINIO_ROOT_USER=fsm_minio
MINIO_ROOT_PASSWORD=changeme_use_a_long_random_string
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
S3_BUCKET=fsm-documents

# --- Backend ---
BACKEND_PORT=3001
JWT_SECRET=changeme_use_a_long_random_string
JWT_EXPIRES_IN=8h
NODE_ENV=production

# Set this to the LAN IP or hostname other devices (technician phones) will use,
# e.g. http://192.168.1.50:3001
PUBLIC_API_URL=http://localhost:3001

# Same idea for photo/document links - this is what browsers use to actually
# download from MinIO. Must be reachable from wherever people view photos
# (their phone, another computer), NOT the internal Docker service name.
PUBLIC_S3_URL=http://localhost:9000

# --- Frontend ---
FRONTEND_PORT=8080

# --- Email (SMTP) - optional for Phase 1, required starting Phase 4 ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Field Service <no-reply@example.com>"

# --- SMS provider (pluggable, Phase 4) ---
SMS_PROVIDER=none

# --- Payment provider (pluggable, Phase 5 customer portal) ---
# "none" means the portal's Pay Invoice button notifies the office instead
# of processing a real charge - no processor is required for the app to work.
PAYMENT_PROVIDER=none

# The customer portal's own URL (used to build invite links in emails).
# Same host/port as PUBLIC_API_URL's frontend counterpart - set this to your
# LAN IP or domain in production, e.g. http://192.168.1.50:8080
PUBLIC_APP_URL=http://localhost:8080

# --- Company info (used on estimate/invoice PDFs until a Settings UI exists) ---
COMPANY_NAME=Your Company Name
COMPANY_ADDRESS=123 Main St, Springfield, IL 62701
COMPANY_PHONE=555-0100
COMPANY_EMAIL=office@example.com
DEFAULT_LABOR_RATE=85
